#include <bits/stdc++.h>

using namespace std;

void solve(){
    
}
int main(){
    freopen("input.inp","r",stdin);
    freopen("output.out","w",stdout);
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> n >> x >> p;
    solve();
    return 0;
}