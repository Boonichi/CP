#include <bits/stdc++.h>

using namespace std;
int n,a[1000000];
void input(){
    cin >> n;
    for (int i = 0; i < n;i++) cin >> a[i];
}
void solve(){
    
}
int main(){
    freopen("input.inp","r",stdin);
    freopen("output.out","w",stdout);
    ios::sync_with_stdio(0);
    cin.tie(0);
    input();
    solve();
    return 0;
}