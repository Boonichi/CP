#include <bits/stdc++.h>

using namespace std;
#define ll long long
long long M = 1e9 + 7;
struct query{
    int l,r,idx;
    bool operator<(Query other) const{
        return make_pair(l/block)
    }
}
int main(){
    freopen("input.inp","r",stdin);
    freopen("output.out","w",stdout);
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> n >> q;
    for (int i = 0; i < n; i++) cin >> a[i];
    while (q--){
        int x,y;
        cin >> x >> y;
        x--; y--;

    }    
    return 0;
}